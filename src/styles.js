import colors from './colors';

const styles = {
  boxShadow: {
    shadowColor: colors.text.gray,
    shadowOffset: {
      width: 0,
      height: 1,
    },
    shadowOpacity: 0.298039,
    shadowRadius: 2,
  },
};

export default styles;
